radio:
	west build -b radio_module app/backplane/radio_module -p auto -DDEBUG=y

radio-sim:
	west build -b native_sim app/backplane/radio_module -p auto -DDEBUG=y

radio-flight:
	west build -b radio_module app/backplane/radio_module -p auto
